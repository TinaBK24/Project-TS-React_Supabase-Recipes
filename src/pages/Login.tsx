import UserAuth from "../components/auth/UserAuth";

const Login = () => {
    return (
        <section className="bg-white flex items-center justify-center p-20">
            <UserAuth />
        </section>
    );
}

export default Login;